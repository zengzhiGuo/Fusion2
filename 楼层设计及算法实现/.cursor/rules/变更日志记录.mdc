---
description: 修改代码时记录变更日志
alwaysApply: false
---
# 修改代码时记录变更日志

## 触发条件

仅当修改以下类型的文件时记录变更日志：
- 前端文件（如 `src/`、`frontend/`、`components/`、`views/` 等目录下的文件）
- 后端文件（如 `backend/`、`server/`、`api/`、`controller/`、`service/`、`models/` 等目录下的文件）

其他类型的文件（如配置文件、测试文件、文档等）修改时无需记录。

## 记录格式

```markdown

- **功能**：简要描述修改的功能
- **文件**：`文件路径`（行号范围）
- **描述**：具体修改内容
- **时间**: 当前系统时间
```

## 示例

```markdown

- **功能**：添加RFID读卡器图标
- **文件**：`src/components/StatusBar.tsx`（45-60行）
- **描述**：在状态栏右侧新增RFID设备状态图标，支持连接/断开两种状态显示
- **时间**: 2026/04/13 10:54
```
