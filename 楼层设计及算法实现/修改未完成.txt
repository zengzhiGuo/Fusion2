路径规划页面在前端 一步一步显示 并用折线连接

## 问题诊断（2024年）

### 发现的问题
1. App.vue文件内容完全重复了两次（从8780行变成应该的3836行）
2. 文件中存在编码问题，导致某些标签不完整
3. 第11行的`<span>`标签缺少结束的`>`符号
4. 文件中有中文乱码（显示为`?????`）

### 已完成的修复
1. ✅ 删除了重复的内容，保留了正确的地图管理页面部分
2. ✅ 创建了备份文件 App.vue.backup
3. ⚠️ 文件仍然无法编译，因为备份文件本身就有编码问题

### 下一步建议
由于备份文件本身存在编码问题，建议：
1. 从一个可以正常工作的版本重新开始
2. 或者手动修复所有编码问题的行
3. 路径规划的逐步显示功能代码已经在文件中，包括：
   - pathPlaybackNodes - 路径播放节点队列
   - visiblePathNodeCount - 当前可见的节点数量
   - startPathPlayback() - 开始路径播放动画
   - pathPlaybackSummary - 显示播放进度
   - PATH_PLAYBACK_INTERVAL_MS = 280ms - 播放间隔

### 功能实现状态
路径规划逐步显示功能的核心代码已经实现，但由于文件编码问题无法编译测试。
