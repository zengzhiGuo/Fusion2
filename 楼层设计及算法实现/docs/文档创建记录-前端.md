# 文档创建记录 - 前端

## 2026/04/13

- **文件路径**：`frontend/package.json`
- **功能**：前端项目配置文件
- **说明**：Vue 3.4 + Vite 5.0 项目，使用 axios 进行 HTTP 请求

---

- **文件路径**：`frontend/vite.config.js`
- **功能**：Vite 构建工具配置
- **说明**：配置路径别名和开发服务器代理（将 /api 请求代理到后端 8082 端口）

---

- **文件路径**：`frontend/index.html`
- **功能**：前端入口 HTML 文件
- **说明**：Vue 应用挂载点

---

- **文件路径**：`frontend/src/main.js`
- **功能**：Vue 应用入口文件
- **说明**：创建并挂载 Vue 应用实例

---

- **文件路径**：`frontend/src/utils/api.js`
- **功能**：HTTP 请求封装
- **说明**：基于 axios 封装请求拦截器和响应拦截器，统一处理错误

---

- **文件路径**：`frontend/src/App.vue`
- **功能**：楼层地图管理主页面组件
- **说明**：从数据库ship_floor.maps读取地图信息，根据imagePath路径展示图片，计算并显示缩放比例，支持点击预览大图，使用 Vue 3 Composition API

---

## 2026/04/14

- **文件路径**：`frontend/src/App.vue`
- **功能**：添加房间楼道区分功能
- **说明**：
  - 新增"添加楼道"按钮（🛤️图标，紫色悬停）
  - 新增 `isAddingCorridor` 状态和 `openCorridorModal`、`onCorridorCoordinateClick` 函数
  - 更新 `doorForm` 添加 `roomType` 字段（1-房间，2-楼道）
  - 更新门详情模态框显示房间类型
  - 添加楼道按钮悬停样式（紫色 #a855f7）

---

- **文件路径**：`frontend/src/components/MapCanvas.vue`
- **功能**：支持房间楼道视觉区分
- **说明**：
  - 新增 `isAddingCorridor` prop
  - 新增 `corridor-click` emit 事件
  - 在 `onMouseDown` 函数中添加楼道点击处理
  - 根据 `roomType` 渲染不同颜色的标记（房间=蓝色，楼道=紫色）
  - 更新 cursor 样式判断包含 `isAddingCorridor`
  - 新增 `.corridor-rect` 样式类（紫色方块）

---

- **文件路径**：`docs/房间楼道区分功能说明.md`
- **功能**：房间楼道区分功能文档
- **说明**：详细说明了房间和楼道的区分实现，包括数据库设计、前后端实现、使用流程和技术要点


## 2026/04/16 11:30

- **文件路径**：`frontend/src/views/StairwayConnectionManager.vue`
- **功能**：楼梯连接管理界面
- **说明**：可视化管理楼梯之间的连接关系，支持为每个楼梯选择上层/下层连接的楼梯，批量保存配置


## 2026/04/17 15:30

- **文件路径**: `docs/路径规划前端集成完成.md`
- **功能**: 路径规划功能前端集成完成总结文档
- **说明**: 记录了路径规划功能集成到前端的详细步骤、修改的文件、使用流程、API接口、故障排查和优化建议
- **时间**: 2026/04/17 15:30


## 2026/04/21

- **文件路径**：`frontend/src/components/MapCanvas.vue`（围栏显示功能）
- **功能**：地图画布组件 - 添加围栏显示功能
- **说明**：
  1. 添加regions和regionCoordinates props接收围栏数据
  2. 在SVG overlay中添加围栏渲染层（最底层）
  3. 使用polygon元素渲染围栏多边形
  4. 在围栏中心显示围栏名称标签
  5. 添加围栏相关辅助函数：getRegionPoints、getRegionPolygonPoints、getRegionCenter
  6. 添加围栏样式：支持自定义颜色、透明度、边框宽度
- **时间**: 2026/04/21
